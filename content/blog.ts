import type { BlogPost } from "./types";

/**
 * No posts yet — the route and types are scaffolded so the Journal can go
 * live by simply pushing entries here (or migrating to a CMS later).
 */
export const blogPosts: BlogPost[] = [];
