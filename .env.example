# Used for canonical URLs, sitemap.xml, robots.txt, and JSON-LD structured data.
# Falls back to the Vercel preview URL below if unset — replace once a real domain is live.
NEXT_PUBLIC_SITE_URL=https://iso-wedding.vercel.app
